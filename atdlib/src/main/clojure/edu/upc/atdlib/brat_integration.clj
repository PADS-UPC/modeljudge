(ns edu.upc.atdlib.brat-integration)

#_"This namespace defines a standalone server application that can be
   used to communicate the BRAT annotation tool with atdlib in order
   to automatically annotate texts."

#_"Based on the useful discussion provided in: https://groups.google.com/forum/#!topic/brat-users/2nP0B81_yd4"

(defn annotation->json
  "Converts an Annotation object to the json response expected by BRAT"
  [annotation])

(defn -main [& args])

